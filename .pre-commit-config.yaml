
  rev: v4.6.0 # Use the latest revision
  hooks:
    - id: trailing-whitespace
    - id: end-of-file-fixer
    - id: check-yaml
    - id: check-json
    - id: check-added-large-files

- repo: github.com # Example HTML linter repo
  rev: 0.1.0 # Replace with a specific, stable revision/tag
  hooks:
    - id: html-linter
      files: \.html$ # Target only HTML files
